{
  "name": "agentos",
  "version": "0.1.0",
  "private": true,
  "description": "Turn any business into an API for AI agents. One company. One CLI. Any agent.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test cli/tests/"
  },
  "keywords": [
    "agents",
    "cli",
    "mcp",
    "neo4j",
    "knowledge-graph"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
